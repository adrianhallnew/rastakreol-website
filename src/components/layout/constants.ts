// 64px bar + 4px StripeMotif (design.md §2.1/§3.2) — shared by BottomNav, StickyAddToCart,
// and ToastProvider so they stay in sync without duplicating the number.
export const BOTTOM_NAV_HEIGHT = 68
